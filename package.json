{
  "name": "m-submit",
  "version": "1.0.0",
  "description": "submission backend",
  "main": "main.js",
  "scripts": {
    "test": "nodemon ./src/server.ts && start http://localhost:3030",
    "clean": "rm -rf dist",
    "html": "cp -r ./src/ui ./dist/ui",
    "build": "npm run clean && tsc -p .",
    "clear-test": "rm -rf ./src/uploads & rm -rf ./src/files"
  },
  "author": "Seiko Santana",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.9",
    "@types/multer": "^1.4.5",
    "express": "^4.17.1",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "copy-files": "^0.1.0",
    "cpy-cli": "^3.1.1",
    "nodemon": "^2.0.6",
    "ts-node": "^9.1.1"
  }
}
